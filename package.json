{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "jquery": "^3.3.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-ga": "^2.5.3",
    "react-scripts": "1.1.5"
  },
  "scripts": {
    "deploy": "npm run build && aws s3 sync build/ s3://jackymoore.com && echo '🚀  Deployed!'",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
